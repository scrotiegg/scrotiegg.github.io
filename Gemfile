source "https://rubygems.org"

# running on github pages? use this gem and let github handle your plugins!
gem "github-pages", group: :jekyll_plugins

# if you want to test local disappointment:
# run: bundle install
# run: bundle exec jekyll serve

# scrotiegg tested, failure guaranteed.
